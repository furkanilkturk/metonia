# Atlas Notes project context

Read [[Project]] and the parent [[Company]] before implementation. Keep work
inside this project unless the user explicitly approves a company-wide change.

The `repository` field in `Project.md` is the canonical remote used to match an
external coding checkout to this context. Verify a proposed knowledge write
against implementation evidence; do not create policy from a one-off task.
