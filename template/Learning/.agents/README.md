# Scoped packs

This directory is intentionally local to Learning. A release vendors
`doc-learning` and `video-learning` under `skills/` from the canonical
`pi-skills` repository. Do not move them to the vault root.
